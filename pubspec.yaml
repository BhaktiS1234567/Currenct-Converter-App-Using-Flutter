name: currency_converter
description: A Flutter project for real-time currency conversion.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4  # Ensure this version is compatible with your SDK version
  camera: ^0.9.4+5
  google_ml_kit: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/img6.jpg
    - assets/img5.jpg
    - assets/img8.jpeg
